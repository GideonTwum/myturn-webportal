# Public API base URL — must match where the NestJS API is served, including /api prefix.
# Local:  http://localhost:3001/api
# Staging: https://api.your-domain.com/api
NEXT_PUBLIC_API_URL=http://localhost:3001/api
